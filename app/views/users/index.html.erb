<h1> All Users 🕺🏿</h1>

<ul>

    <% @users.each do |user| %>
     <li>  <%= user.name %> - <%= button_to "#{user.name}'s Page", user_path(user), method: :get %> </li> <br>
    <% end %>

<ul>



